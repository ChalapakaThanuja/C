#include<stdio.h>
void main()
{
int a,b;
printf("enter two number");
scanf("%d%d",&a,&b);
int max=a>b?a:b;
int step=max;
while(2)
{
    if(max%a==0 && max%b==0)
    {
        printf("he LCM is %d",max);
        break;
    }
    else
        {
        max=max+step;
        }
}
}
